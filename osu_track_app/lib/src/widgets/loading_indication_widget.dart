import 'package:flutter/material.dart';

class LoadingIndicationWidget extends StatelessWidget {
  const LoadingIndicationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
//soon